version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ""
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    container_name: suggestionbox-app
